#include "Tree.h"
#include<cstdio>
#include<vector>

using namespace std;

Tree::Tree(Node x) {
    left = NULL;
    right = NULL;
    node = Node(x.getNode());
}

void Tree::addNewNodeToTree(Node x) {

}
